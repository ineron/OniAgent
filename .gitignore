bin/
obj/

# Contains a live Ledgyx API key once the mod runs — never committed.
settings.json

# Vendored reference assemblies — copied per-machine from the game install,
# not part of the mod's own source. See lib/README.md.
lib/*.dll
