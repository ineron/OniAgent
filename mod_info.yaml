# ONI Mod Manager metadata. Ships alongside OniAgent.dll in the installed mod
# folder. minimumSupportedBuild is a placeholder — update it once we know
# which build this was actually verified against, if a build gate matters.
requiredDlcIds: []
forbiddenDlcIds: []
minimumSupportedBuild: 0
version: 0.1.0
APIVersion: 2
