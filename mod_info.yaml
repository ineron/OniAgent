# ONI Mod Manager metadata. Ships alongside OniAgent.dll in the installed mod
# folder. Values below are placeholders — verify/update against the actual
# game build via the in-game Mod Manager on the Mac test machine before
# first install; an incorrect minimumSupportedBuild can cause silent
# failure to load.
supportedContent: All
minimumSupportedBuild: 0
version: 0.1.0
APIVersion: 2
